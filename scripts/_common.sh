#!/bin/bash

#=================================================
# COMMON VARIABLES AND CUSTOM HELPERS
#=================================================

# Vantage is a single vendored static build (sources/index.html) with no
# server process of its own — there's nothing to configure beyond copying
# the file and pointing nginx at it, so no helpers are needed here yet.
